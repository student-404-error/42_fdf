/*#include "data_structure.h"
#include <stdio.h>
#include <stdlib.h>

int	main(void)
{
	t_queue	*q;
	int	data;

	q = init_queue();
	enqueue(q, 10);
	enqueue(q, 5);
	enqueue(q, 3);
	data = dequeue(q);
	printf("dequeue: %d\n", data);
//	free(p);
//	p = dequeue(q);
//	printf("dequeue: %d\n", p->data);
//	free(p);
//	p = dequeue(q);
//	printf("dequeue: %d\n", p->data);
//	free(p);
	ft_free_queue(q);
}*/
