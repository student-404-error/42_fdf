자 지금 문제가 무엇인가.

자 지금 문제는 기울기가 음수일 때 그리지 못 함.
그리고 기울기가 1보다 클 때 끝 맺음을 잘 못 함.

이거 해결하는 법 =>
솔직히 그냥 기울기
	m < -1일 때
	[-1, 0)일 때
	m > 1일 때
	(0, 1]일 때
함수를 다 따로 만들어서 하면 쌉 가능 개 쉽게 가능.

하나의 bresenham 으로 기울기 m < -1, -1 <= m < 0, 0 < m <= 1, 1 < m를 구현할 수는 없을까?

일단 0 < m <= 1이랑 1 < m은 쉬워.

공부하는 방법을 찾았다.
구현해라. 걍.
테스트는 돌려보면 되는거니까. 
그르네 그냥 다 돌려보면 그만이고~
okay 기기
